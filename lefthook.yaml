commit-msg:
  commands:
    "lint commit message":
      use_stdin: true
      run: cat {1} | commitlint

pre-push:
  parallel: true
  jobs:
    - name: audit
      run: task audit
    - name: run-tests
      run: task test